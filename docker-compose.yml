services:
  etl:
    build: .
    container_name: etl
    volumes:
      # Mount entire project directory
      - .:/app
    stdin_open: true
    tty: true
    # Enable GPU support
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]